import { PageHero, PriceList, CTASection } from '../../components/Site';
import { categories, priceDisclaimer } from '../../lib/services';
import { seo } from '../../lib/seo';
export const metadata=seo('Beauty Treatment Prices Hyde | NG Aesthetics Beauty Lab','Browse our full beauty treatment price list: facials, skin boosters, makeup, brows, threading and waxing in Hattersley, Hyde. Enquire to book.','/prices');
export default function Prices(){return <main id="main"><PageHero label="YOUR BEAUTY MENU" title="A little luxury. Clearly priced." copy="Explore every treatment in one place. Choose a category below and find the right appointment for you."/><section className="section pricing-section"><div className="price-categories" aria-label="Jump to price category">{categories.map(c=><a href={`#${c.slug}`} key={c.slug}>{c.slug==='beauty-treatments'?'Brows & Tinting':c.slug==='threading-tinting'?'Threading':c.name}</a>)}</div><div className="accordions">{categories.map(c=><details key={c.slug} id={c.slug} open><summary><span>{c.slug==='beauty-treatments'?'Brows & Tinting':c.slug==='threading-tinting'?'Threading':c.name}</span><span className="summary-detail">{c.treatments.length} options <span aria-hidden="true">−</span></span></summary><PriceList category={c}/>{c.note&&<p className="accordion-note">{c.note}</p>}</details>)}</div><p className="fineprint">{priceDisclaimer}</p></section><CTASection/></main>}
