
import { useState } from 'react';
import { business } from '../lib/business';
import { categories } from '../lib/services';
export default function ContactForm(){const [ready,setReady]=useState('');return <form className="contact-form" onChange={()=>setReady('')} onSubmit={e=>{e.preventDefault();const form=e.currentTarget;if(!form.reportValidity())return;const d=new FormData(form);const message=`Hi NG Aesthetics & Beauty Lab, I'd like to enquire about booking.\nName: ${d.get('name')}\nPhone: ${d.get('phone')}\nEmail: ${d.get('email')||'Not provided'}\nTreatment: ${d.get('treatment')}\nPreferred date: ${d.get('date')||'Flexible'}\nMessage: ${d.get('message')||''}`;setReady(`${business.whatsapp}?text=${encodeURIComponent(message)}`);}}><h2>Let’s plan your visit.</h2><p>Complete your details to prepare a WhatsApp enquiry. You’ll review and send the message in WhatsApp.</p><div className="form-grid"><label>Name <span>*</span><input name="name" required autoComplete="name" maxLength={100}/></label><label>Phone <span>*</span><input name="phone" type="tel" required autoComplete="tel" minLength={7} maxLength={25} pattern="[+0-9 ()-]{7,25}" title="Enter a phone number using digits, spaces, +, brackets or hyphens"/></label><label>Email <small>(optional)</small><input name="email" type="email" autoComplete="email" maxLength={150}/></label><label>Treatment Interested In <span>*</span><select name="treatment" required defaultValue=""><option value="" disabled>Select a treatment</option>{categories.map(c=><option key={c.slug}>{c.name}</option>)}</select></label><label>Preferred Date <small>(optional)</small><input name="date" type="date" min={new Date().toLocaleDateString('en-CA')}/></label><label className="full">Message <small>(optional)</small><textarea name="message" rows={4} maxLength={1500} placeholder="Tell us which treatment you’re interested in. Please don’t include medical or sensitive information."/></label></div><p className="form-note">Your enquiry is not stored by this website. When you continue, your entered details are shared with WhatsApp. Read our <a href="/privacy">Privacy Policy</a>. Sending an enquiry does not confirm an appointment.</p><button className="button" type="submit">Prepare WhatsApp Enquiry ↗</button><div aria-live="polite">{ready&&<div className="form-ready"><p>Your message is ready. Continue to WhatsApp to review it and press Send.</p><a className="button" href={ready} target="_blank" rel="noopener noreferrer">Continue to WhatsApp ↗</a></div>}</div></form>}
