
import { useState } from 'react';

import { bookingUrl } from '../lib/business';
export const links = [['Home','/'],['About','/about'],['Treatments','/treatments'],['Prices','/prices'],['Gallery','/gallery'],['Contact','/contact']];
export default function Header({pathname}:{pathname:string}){const [open,setOpen]=useState(false);const path=pathname;return <><a className="skip-link" href="#main">Skip to content</a><div className="topbar">A little self-care, close to home. <span>HATTERSLEY, HYDE</span></div><header className="header"><a className="brand" href="/" aria-label="NG Aesthetics & Beauty Lab home"><img src="/images/logo.jpg" alt="NG Aesthetics & Beauty Lab logo" width="68" height="68"/><span>NG Aesthetics<small>& BEAUTY LAB</small></span></a><button className="menu-toggle" type="button" aria-expanded={open} aria-controls="navigation" onClick={()=>setOpen(!open)}>{open?'Close ✕':'Menu ☰'}</button><nav id="navigation" className={open?'nav open':'nav'} aria-label="Main navigation" onKeyDown={e=>{if(e.key==='Escape')setOpen(false)}}>{links.map(([name,url])=><a key={url} href={url} aria-current={path===url?'page':undefined} onClick={()=>setOpen(false)}>{name}</a>)}<a className="button small" href={bookingUrl()}>Book Appointment ↗</a></nav></header></>}
